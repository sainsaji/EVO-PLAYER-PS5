/* =============================================================================
 * EVO PLAYER PRO - Launch / Home screen
 *
 * Geometry follows ui/include/evo_metrics.h so a host render and a console
 * capture line up under shot.py diff, with one deliberate shift: the legacy
 * screen drew no navigation rail (page.no_rail), and this one sits beside the
 * 96 px navbar.rml overlay like every other RmlUi screen. So the content
 * column starts at EVO_CONTENT_X (152) rather than the 88 px bleed, and is
 * EVO_CONTENT_W (1704) wide like every other screen. Tiles are 264 + 24 gap:
 * 6*264 + 5*24 = 1704 exactly.
 * ============================================================================= */

body {
    font-family: LatoLatin;
    font-weight: normal;
    font-size: 16dp;
    color: #ffffff;
    background-color: #06090e;
    margin: 0;
    padding: 0;
    width: 1920dp;
    height: 1080dp;
    overflow: hidden;
    position: relative;
}

div, span, p {
    box-sizing: border-box;
}

#launch-pane {
    position: absolute;
    left: 152dp;
    top: 0;
    width: 1768dp;
    height: 1080dp;
}

/* =============================== Header =============================== */

#launch-header {
    position: absolute;
    left: 0;
    top: 72dp;
    width: 1704dp;
    height: 88dp;
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
}

#brand {
    display: flex;
    flex-direction: row;
    align-items: center;
}

/* The accent bloom behind the mark, standing in for evo_ui_circle(). */
#brand-mark {
    width: 76dp;
    height: 76dp;
    border-radius: 38dp;
    background-color: #ffcd001a;
    border-width: 1dp;
    border-color: #ffcd0038;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-right: 18dp;
}

#brand-logo {
    width: 40dp;
    height: 40dp;
    display: inline-block;
}

#brand-text {
    display: flex;
    flex-direction: column;
}

#brand-name {
    font-size: 32dp;
    font-weight: bold;
    color: #eef4ff;
    letter-spacing: 3dp;
}

#brand-version {
    font-size: 14dp;
    font-weight: bold;
    color: #5e718c;
    letter-spacing: 2.5dp;
    margin-top: 5dp;
}

#status {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
}

#status-clock {
    font-size: 32dp;
    font-weight: bold;
    color: #eef4ff;
    letter-spacing: 1.5dp;
}

#status-theme-wrap {
    display: flex;
    flex-direction: row;
    align-items: center;
    margin-top: 5dp;
}

#status-theme-dot {
    width: 6dp;
    height: 6dp;
    border-radius: 3dp;
    background-color: #00cdff;
    margin-right: 8dp;
}

#status-theme {
    font-size: 14dp;
    font-weight: bold;
    color: #5e718c;
    letter-spacing: 2.5dp;
}

/* ================================ Hero ================================ */

#hero {
    position: absolute;
    left: 0;
    top: 200dp;
    width: 1704dp;
    height: 318dp;
    background-color: #0f1829;
    border-width: 1dp;
    border-color: #5a7db433;
    border-radius: 22dp;
    overflow: hidden;
}

/*
 * Cover-crop, not stretch: the source is 16:9 and the hero is closer to
 * 5.5:1, so fitting the box makes faces visibly wide. The image decorator
 * with `cover` is the retained-mode equivalent of the fixed-point centre-crop
 * in draw_hero(); an oversized <img> clipped by overflow is not, because
 * overflow does not clip absolutely-positioned children here — it painted
 * the poster across the whole page.
 */
@keyframes heroKen {
    from { transform: scale(1.02); }
    to   { transform: scale(1.10); }
}

#hero-art {
    position: absolute;
    left: 1dp;
    top: 1dp;
    width: 1702dp;
    height: 316dp;
    border-radius: 21dp;
    animation: 20s cubic-in-out infinite alternate heroKen;
}

/*
 * Artwork fades out towards the left so the copy sits on flat surface colour
 * while the frame still reads as a backdrop. A region that simply starts
 * partway across leaves a hard vertical seam down the middle of the card.
 */
#hero-fade-l {
    position: absolute;
    left: 1dp;
    top: 1dp;
    width: 520dp;
    height: 316dp;
    background-color: #0f1829;
}

#hero-fade-h {
    position: absolute;
    left: 521dp;
    top: 1dp;
    width: 600dp;
    height: 316dp;
    decorator: horizontal-gradient(#0f1829ff #0f182900);
}

/* Bottom scrim, so the chip and the bar sit on a consistent ground. */
#hero-fade-b {
    position: absolute;
    left: 1dp;
    bottom: 1dp;
    width: 1702dp;
    height: 170dp;
    decorator: vertical-gradient(#02040a00 #02040add);
}

#hero-copy {
    position: absolute;
    left: 56dp;
    top: 38dp;
    width: 900dp;
    display: flex;
    flex-direction: column;
}

#hero-eyebrow-wrap {
    display: flex;
    flex-direction: row;
    align-items: center;
}

#hero-eyebrow-dot {
    width: 6dp;
    height: 6dp;
    border-radius: 3dp;
    background-color: #ffcd00;
    margin-right: 10dp;
}

#hero-eyebrow {
    font-size: 15dp;
    font-weight: bold;
    color: #ffcd00;
    letter-spacing: 2.6dp;
}

#hero-title {
    font-size: 44dp;
    font-weight: bold;
    color: #ffffff;
    letter-spacing: 0.5dp;
    margin-top: 12dp;
    white-space: nowrap;
    text-overflow: ellipsis;
    overflow: hidden;
}

#hero-detail {
    font-size: 19dp;
    font-weight: normal;
    color: #a3b6d0;
    letter-spacing: 0.5dp;
    margin-top: 14dp;
    white-space: nowrap;
    text-overflow: ellipsis;
    overflow: hidden;
}

#hero-foot {
    position: absolute;
    left: 56dp;
    bottom: 28dp;
    width: 800dp;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
}

#hero-progress-track {
    width: 560dp;
    height: 5dp;
    border-radius: 3dp;
    background-color: #7896c833;
    margin-bottom: 18dp;
}

/* Explicitly block: an inline element ignores width, so the fill drew
 * nothing and the bar read as an empty track at every position. */
#hero-progress-fill {
    display: block;
    width: 0%;
    height: 5dp;
    border-radius: 3dp;
    background-color: #ffcd00;
    transition: width 0.35s cubic-out;
}

/*
 * Chip geometry with smooth transitions matching ui.html
 */
#hero-chip {
    height: 58dp;
    border-radius: 29dp;
    background-color: #162238eb;
    border-width: 1dp;
    border-color: #00cdff47;
    display: inline-flex;
    flex-direction: row;
    align-items: center;
    padding-left: 14dp;
    padding-right: 20dp;
    transition: transform 0.2s cubic-out, background-color 0.2s cubic-out, border-color 0.2s cubic-out;
}

#hero.hero-focused #hero-chip,
.hero-chip-focused {
    transform: scale(1.04);
    background-color: #1e2e4af5;
    border-color: #00cdff;
}

#hero-chip-glyph-box {
    width: 34dp;
    height: 34dp;
    border-radius: 17dp;
    background-color: #00cdff1f;
    border-width: 1dp;
    border-color: #00cdff73;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-right: 12dp;
}

#hero-chip-glyph {
    width: 18dp;
    height: 18dp;
    display: inline-block;
}

#hero-chip-label {
    font-size: 21dp;
    font-weight: bold;
    color: #eef4ff;
    letter-spacing: 2dp;
}

/* =============================== Shelves =============================== */

#shelves {
    position: absolute;
    left: 0;
    top: 556dp;
    width: 1704dp;
    display: flex;
    flex-direction: column;
}

/* 36 px label band + 166 px tiles = 202, and 26 of air below, so the second
 * shelf lands at 784 and its tiles end at 986 — clear of the footer. Hiding
 * the first shelf reflows the second up into its slot, which is what
 * draw_launch() does by hand. */
.shelf {
    width: 1704dp;
    margin-bottom: 26dp;
}

.shelf-head {
    width: 1704dp;
    height: 36dp;
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
}

.shelf-label {
    display: flex;
    flex-direction: row;
    align-items: center;
}

.shelf-tick {
    width: 3dp;
    height: 18dp;
    border-radius: 2dp;
    background-color: #ffcd00;
    margin-right: 14dp;
}

.shelf-title {
    font-size: 22dp;
    font-weight: bold;
    color: #c2d2e8;
    letter-spacing: 2.2dp;
}

.shelf-count {
    font-size: 14dp;
    font-weight: bold;
    color: #5e718c;
    letter-spacing: 2dp;
}

.shelf-tiles {
    width: 1704dp;
    height: 166dp;
    display: flex;
    flex-direction: row;
    gap: 24dp;
}

/* ================================ Tiles ================================ */

.tile {
    width: 264dp;
    height: 166dp;
    background-color: #101a2ceb;
    border-width: 1dp;
    border-color: #5a7db433;
    border-radius: 18dp;
    position: relative;
    overflow: hidden;
    transition: transform 0.18s cubic-out, border-color 0.18s cubic-out, background-color 0.18s cubic-out;
}

.tile-focused {
    background-color: #16223af5;
    border-width: 2dp;
    border-color: #ffcd00;
    transform: scale(1.04);
}

.tile-destination {
    background-color: #0f1828eb;
}

.tile-destination.tile-focused {
    background-color: #16223af5;
    border-width: 2dp;
    border-color: #00cdff;
    transform: scale(1.04);
}

.tile-art {
    position: absolute;
    left: 0;
    top: 0;
    right: 0;
    bottom: 0;
    border-radius: 18dp;
}

.tile-index {
    position: absolute;
    right: 16dp;
    top: 18dp;
    z-index: 3;
    font-size: 12dp;
    font-weight: bold;
    letter-spacing: 2dp;
    color: #a0bee659;
}

/* Destination tiles carry an icon box top-left. */
.tile-icon-box {
    position: absolute;
    left: 18dp;
    top: 14dp;
    display: flex;
    align-items: center;
    justify-content: center;
}

.tile-icon {
    width: 36dp;
    height: 36dp;
    display: inline-block;
}

.tile-icon-box-lg {
    position: absolute;
    left: 20dp;
    top: 20dp;
    z-index: 3;
    width: 52dp;
    height: 52dp;
    border-radius: 14dp;
    background-color: #00cdff14;
    border-width: 1dp;
    border-color: #00cdff33;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background-color 0.18s cubic-out, border-color 0.18s cubic-out;
}

.tile.tile-focused .tile-icon-box-lg {
    background-color: #00cdff26;
    border-color: #00cdff;
}

.tile-icon-lg {
    width: 30dp;
    height: 30dp;
}

.tile-status {
    position: absolute;
    right: 16dp;
    bottom: 20dp;
    z-index: 3;
    width: 7dp;
    height: 7dp;
    border-radius: 4dp;
    background-color: #3ddc84;
    display: none;
}

#lib-status-0,
#lib-status-3 {
    display: block;
}

/* Without this a bright frame makes the caption unreadable. */
.tile-scrim {
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    height: 110dp;
    z-index: 2;
    decorator: vertical-gradient(#00000000 #000000f0);
}

.tile-caption {
    position: absolute;
    left: 18dp;
    right: 18dp;
    bottom: 16dp;
    z-index: 3;
    display: flex;
    flex-direction: column;
}

.tile-title {
    font-size: 20dp;
    font-weight: bold;
    color: #ffffff;
    letter-spacing: 0.4dp;
    white-space: nowrap;
    text-overflow: ellipsis;
    overflow: hidden;
}

.tile-detail {
    font-size: 15dp;
    font-weight: normal;
    color: #c3d2e6;
    letter-spacing: 0.6dp;
    margin-top: 5dp;
    white-space: nowrap;
    text-overflow: ellipsis;
    overflow: hidden;
}

.tile-progress-track {
    position: absolute;
    left: 18dp;
    right: 18dp;
    bottom: 8dp;
    height: 5dp;
    z-index: 4;
    border-radius: 3dp;
    background-color: #7896c838;
}

.tile-progress-fill {
    display: block;
    width: 0%;
    height: 5dp;
    border-radius: 3dp;
    background-color: #ffcd00;
}

.tile-badge {
    position: absolute;
    right: 14dp;
    top: 14dp;
    z-index: 3;
    height: 24dp;
    padding: 0 10dp;
    border-radius: 12dp;
    background-color: #060c16c7;
    border-width: 1dp;
    border-color: #7896c847;
    display: flex;
    align-items: center;
    font-size: 11dp;
    font-weight: bold;
    color: #a3b6d0;
    letter-spacing: 1.4dp;
}

/* =============================== Footer =============================== */

#launch-footer {
    position: absolute;
    left: 0;
    bottom: 40dp;
    width: 1704dp;
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 32dp;
}

.footer-hint {
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 10dp;
}

.footer-glyph {
    width: 26dp;
    height: 26dp;
    display: inline-block;
}

.glyph-badge {
    width: 38dp;
    height: 38dp;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 19dp;
    background-color: #14223ab8;
    border-width: 1dp;
    border-color: #7896c838;
}

.glyph-badge-img {
    width: 24dp;
    height: 24dp;
}

.footer-text {
    font-size: 14dp;
    font-weight: bold;
    color: #8b99ad;
    letter-spacing: 1.8dp;
}
